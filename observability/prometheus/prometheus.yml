global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: company-check-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:8080]
        labels:
          service: company-check-service
